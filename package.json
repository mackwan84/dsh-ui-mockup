{
  "name": "dsh-ui-mockup",
  "private": true,
  "version": "0.1.0-rc.0",
  "description": "DSH 插件：研讨阶段生成 UI 草图/高保真设计稿，确认后锁定设计规格",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "typecheck": "pnpm -r exec tsc -p tsconfig.json --noEmit",
    "pack": "rm -rf dist && mkdir -p dist && cd packages/image && pnpm pack --pack-destination ../../dist && cd ../image-dashscope && pnpm pack --pack-destination ../../dist && cd ../tool-ui-mockup && pnpm pack --pack-destination ../../dist && cd ../../bundle/ui-mockup && pnpm pack --pack-destination ../../dist"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/cordis-plugin-include": "^1.0.6",
    "@deepseek-ai/cordis-plugin-loader": "^1.0.2",
    "@mackwan84/dsh-image": "workspace:^",
    "@mackwan84/dsh-image-dashscope": "workspace:^",
    "@mackwan84/dsh-tool-ui-mockup": "workspace:^",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
