# v0.3.0 Release Record

[中文](../../releases/v0.3.0.md)

> Record date: 2026-09-17
> Acceptance candidate commit: `83cc6cc` (merged into local `develop` through `294f5d4`)
> Formal release commit: `9107195cc22fae326a44135afeddd1cb53026053`
> Git tag: [v0.3.0](https://github.com/mackwan84/dsh-ui-mockup/releases/tag/v0.3.0)
> Status: **formally released.**

## Candidate acceptance conclusion

- P0: 94 passed, 1 not applicable under the single-workspace acceptance constraint, 0 failed, 0 blocked, and 0 unexecuted.
- DashScope, Volcengine, and OpenAI-compatible gateway each completed isolated UI credential entry, real generation, and UI clearing.
- Volcengine only guarantees high-fidelity images and whole-image editing. `fidelity=wireframe` is rejected at the tool layer with provider-switch guidance.
- Browser coverage includes annotation deletion, anchors, provider reference and automatic-anchor flows, language/theme/viewport matrix, and timing boundaries.
- Raw screenshots, request samples, and service logs remain in uncommitted `.artifacts/` and contain no credential values.

## Engineering gates

| Check                                                         | Result                                |
| ------------------------------------------------------------- | ------------------------------------- |
| `pnpm test`                                                   | 14 test files, 301/301 passing        |
| `pnpm typecheck`                                              | Passed                                |
| `pnpm lint`                                                   | Passed                                |
| `pnpm format:check`                                           | Passed                                |
| `pnpm build`                                                  | Passed                                |
| `pnpm pack:all`                                               | Passed; produced six `0.3.0` tarballs |
| `npm publish ./dist/<package>.tgz --dry-run` for all packages | Passed                                |
| Install all six tarballs in a fresh temporary directory       | Passed; every package reports `0.3.0` |

## Formal release facts

- Release [PR #8](https://github.com/mackwan84/dsh-ui-mockup/pull/8) merged; the remote `main` commit is `9107195cc22fae326a44135afeddd1cb53026053`.
- One set of six tarballs was rebuilt from an isolated worktree at that commit. Every package passed `npm publish --dry-run` and local tarball-closure installation.
- All six packages were published in dependency order and have npm `latest` set to `0.3.0`. Their registry `dist.shasum` values are:
  - `@mackwan84/dsh-image`: `23871294ba649599b4cb923ac6e9f71f9364c1f9`;
  - `@mackwan84/dsh-image-dashscope`: `db7fec4d8d455d121a6216dc6f3cfbb82eedd7fd`;
  - `@mackwan84/dsh-image-volcengine`: `eac989d0046307d0dfc7048548f3d0cdfa86f06a`;
  - `@mackwan84/dsh-image-openai-compat`: `dde0ab519018a99141d6639ce509e58fdf3999af`;
  - `@mackwan84/dsh-tool-ui-mockup`: `b29f1613f7ecdd2cf75d326cd420d4b7b93e0bf8`;
  - `@mackwan84/dsh-ui-mockup-bundle`: `d9f011d67b66df37b46a02553fc572523ff7ec25`.
- A fresh public temporary directory installed `@mackwan84/dsh-ui-mockup-bundle@0.3.0` from `https://registry.npmjs.org`; all six packages resolved to `0.3.0`, and `npm audit --omit=dev --audit-level=high` found zero vulnerabilities.
- The annotated `v0.3.0` tag points at the same commit and is pushed. The GitHub Release is published, not a draft or prerelease.

## Known verification boundary

- Browser UI acceptance evidence was collected at candidate commit `83cc6cc`. No product-code files changed from `83cc6cc` through the pre-merge candidate `2c2f35e`, and remote `main` has the same source tree; this release rebuilt that tree. No additional post-release interactive UI capture was taken.
