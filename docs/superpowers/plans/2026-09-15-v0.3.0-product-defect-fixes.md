# v0.3.0 Product Defect Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 OpenAI 兼容模型空值、单页历史总数缺失和方向稿首次精修错误携带参考图三个已确认缺陷。

**Architecture:** 三个缺陷分别在 Provider 响应归一、历史页面呈现和卡片模型消息边界内修复，不改变跨模块接口。每项独立执行红绿测试、同步当前行为文档并原子提交。

**Tech Stack:** TypeScript、React、Vitest、Testing Library、Prettier、ESLint、pnpm。

**Spec:** `docs/superpowers/specs/2026-09-15-v0.3.0-acceptance-repair-design.md`

## Global Constraints

- 不新增生产依赖，不扩大 OpenAI 兼容最小协议子集。
- 代码注释使用中文，只解释业务意图或非显然原因。
- 每个缺陷必须先看到针对现状的预期失败，再修改生产代码。
- 每项修复独立提交，提交信息遵循 Conventional Commits 中文描述。
- 用户行为、错误语义或架构事实变化必须在同一提交更新对应文档。
- 不执行发布、推送或标签操作。

---

## File Structure

- `packages/image-openai-compat/src/index.ts`：OpenAI 兼容响应解析与请求模型回落。
- `packages/image-openai-compat/tests/provider.spec.ts`：Provider 返回模型优先级测试。
- `packages/tool-ui-mockup/src/client/settings-panel.tsx`：历史总数和分页呈现。
- `packages/tool-ui-mockup/tests/settings-panel.client.spec.tsx`：历史组件测试。
- `packages/tool-ui-mockup/src/client/locales.ts`：方向稿精修固定中文消息。
- `packages/tool-ui-mockup/tests/toolview.client.spec.tsx`：精修消息参数约束测试。
- `docs/architecture/overview.md`、`docs/guides/product-guide.md`、`docs/testing/v0.3.0/browser-cases.md`：当前行为和验收断言。

### Task 1: OpenAI 兼容响应模型回落

**Files:**

- Modify: `packages/image-openai-compat/tests/provider.spec.ts`
- Modify: `packages/image-openai-compat/src/index.ts`
- Modify: `docs/architecture/overview.md`
- Modify: `docs/testing/v0.3.0/browser-cases.md`

**Interfaces:**

- Consumes: `OpenaiCompatImageProvider.generate(spec, signal)`，最终请求体 `body.model` 是非空字符串。
- Produces: `ImageGenerateResult.model: string`；响应模型非空时优先，否则回落请求模型。

- [ ] **Step 1: 添加缺失模型回落的失败测试**

在“响应归一（双格式）”分组增加：

```ts
it('成功响应省略或留空 model 时回落到本次请求模型', async () => {
  const responses = [
    JSON.stringify({ data: [{ b64_json: 'aWNl' }] }),
    JSON.stringify({ model: '   ', data: [{ b64_json: 'aWNl' }] }),
  ]
  for (const response of responses) {
    mockFetch(() => new Response(response, { status: 200 }))
    const result = await provider().generate({ ...wireframeSpec, model: 'requested-image-model' })
    expect(result.model).toBe('requested-image-model')
  }
})
```

- [ ] **Step 2: 运行测试并确认按预期失败**

```bash
pnpm exec vitest run packages/image-openai-compat/tests/provider.spec.ts -t '成功响应省略或留空 model 时回落到本次请求模型'
```

Expected: FAIL；当前实际值为空串或纯空白，不是 `requested-image-model`。

- [ ] **Step 3: 实施最小模型回落**

在 `postImages` 成功解析处使用：

```ts
const responseModel = typeof data.model === 'string' ? data.model.trim() : ''
const requestedModel = typeof body.model === 'string' ? body.model : ''
const model = responseModel !== '' ? responseModel : requestedModel
```

保持图片解析和错误分支不变。

- [ ] **Step 4: 增加响应模型优先测试并运行 Provider 全组**

```ts
it('成功响应提供非空 model 时以响应模型为准', async () => {
  mockFetch(
    () =>
      new Response(JSON.stringify({ model: 'gateway-model', data: [{ b64_json: 'aWNl' }] }), {
        status: 200,
      }),
  )
  const result = await provider().generate({ ...wireframeSpec, model: 'requested-model' })
  expect(result.model).toBe('gateway-model')
})
```

```bash
pnpm exec vitest run packages/image-openai-compat/tests/provider.spec.ts
```

Expected: PASS；无新增警告。

- [ ] **Step 5: 同步文档并提交**

在架构说明补充模型回落，在 `OAI-01`/`GEN-01` 明确结果与历史显示请求模型。

```bash
pnpm exec prettier --check packages/image-openai-compat/src/index.ts packages/image-openai-compat/tests/provider.spec.ts docs/architecture/overview.md docs/testing/v0.3.0/browser-cases.md
git diff --check
git add packages/image-openai-compat/src/index.ts packages/image-openai-compat/tests/provider.spec.ts docs/architecture/overview.md docs/testing/v0.3.0/browser-cases.md
git commit -m "fix(image-openai-compat): 响应缺少模型时回落请求模型"
```

### Task 2: 单页历史显示总条数

**Files:**

- Modify: `packages/tool-ui-mockup/tests/settings-panel.client.spec.tsx`
- Modify: `packages/tool-ui-mockup/src/client/settings-panel.tsx`
- Modify: `docs/guides/product-guide.md`
- Modify: `docs/architecture/overview.md`
- Modify: `docs/testing/v0.3.0/browser-cases.md`

**Interfaces:**

- Consumes: `history/list` 的 `total/page/pageSize/entries`。
- Produces: 所有加载完成状态均显示 `panel.history.totalCount`；仅多页显示翻页控件。

- [ ] **Step 1: 添加单页与空态失败测试**

```tsx
it.each([
  [0, '共 0 条'],
  [1, '共 1 条'],
  [5, '共 5 条'],
])('单页或空态 total=%i 时仍显示总条数且不显示分页按钮', async (historyTotal, copy) => {
  mountPanel({ historyTotal })
  fireEvent.click(screen.getByRole('tab', { name: '生成历史' }))

  expect(await screen.findByText(copy)).toBeTruthy()
  expect(screen.queryByRole('button', { name: '上一页' })).toBeNull()
  expect(screen.queryByRole('button', { name: '下一页' })).toBeNull()
})
```

- [ ] **Step 2: 运行测试并确认单页断言失败**

```bash
pnpm exec vitest run packages/tool-ui-mockup/tests/settings-panel.client.spec.tsx -t '单页或空态'
```

Expected: FAIL；`共 N 条` 当前只在 `totalPages > 1` 时渲染。

- [ ] **Step 3: 把总数从分页条件中拆出**

始终渲染汇总容器和总数，只条件渲染页码按钮：

```tsx
<div
  className="ui-mockup-history-pagination"
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 8,
    flexWrap: 'wrap',
  }}
>
  <span style={{ fontSize: 12, color: tokens.labelTertiary }}>
    {t('panel.history.totalCount', { n: total })}
  </span>
  {totalPages > 1 && (
    <div
      className="ui-mockup-history-pages"
      style={{ display: 'flex', alignItems: 'center', gap: 4 }}
    >
      <Button
        variant="ghost"
        size="sm"
        disabled={page <= 1}
        onClick={() => void reload(appliedQuery, page - 1, draftOnly)}
      >
        {t('panel.history.prev')}
      </Button>
      {Array.from({ length: totalPages }, (_, i) => i + 1).map((p) => (
        <Button
          key={p}
          variant={p === page ? 'primary' : 'ghost'}
          size="sm"
          className="ui-mockup-history-page-number"
          onClick={() => void reload(appliedQuery, p, draftOnly)}
        >
          {p}
        </Button>
      ))}
      <Button
        variant="ghost"
        size="sm"
        disabled={page >= totalPages}
        onClick={() => void reload(appliedQuery, page + 1, draftOnly)}
      >
        {t('panel.history.next')}
      </Button>
    </div>
  )}
</div>
```

不得修改 `reload`、页码钳制或迟到响应逻辑。

- [ ] **Step 4: 增加多页保护并运行设置面板全组**

```tsx
it('多页同时显示总条数和分页按钮', async () => {
  mountPanel({ historyTotal: 6 })
  fireEvent.click(screen.getByRole('tab', { name: '生成历史' }))

  expect(await screen.findByText('共 6 条')).toBeTruthy()
  expect(screen.getByRole('button', { name: '上一页' })).toBeTruthy()
  expect(screen.getByRole('button', { name: '下一页' })).toBeTruthy()
})
```

```bash
pnpm exec vitest run packages/tool-ui-mockup/tests/settings-panel.client.spec.tsx
```

Expected: PASS。

- [ ] **Step 5: 同步文档并提交**

产品指南和架构说明写明“空态、单页、多页均显示过滤后的总条数；分页只在多页出现”，并补充 `HIS-12` 断言。

```bash
pnpm exec prettier --check packages/tool-ui-mockup/src/client/settings-panel.tsx packages/tool-ui-mockup/tests/settings-panel.client.spec.tsx docs/guides/product-guide.md docs/architecture/overview.md docs/testing/v0.3.0/browser-cases.md
git diff --check
git add packages/tool-ui-mockup/src/client/settings-panel.tsx packages/tool-ui-mockup/tests/settings-panel.client.spec.tsx docs/guides/product-guide.md docs/architecture/overview.md docs/testing/v0.3.0/browser-cases.md
git commit -m "fix(tool-ui-mockup): 单页历史显示总条数"
```

### Task 3: 约束方向稿精修调用参数

**Files:**

- Modify: `packages/tool-ui-mockup/tests/toolview.client.spec.tsx`
- Modify: `packages/tool-ui-mockup/src/client/locales.ts`
- Modify: `docs/guides/product-guide.md`
- Modify: `docs/architecture/overview.md`
- Modify: `docs/testing/v0.3.0/browser-cases.md`

**Interfaces:**

- Consumes: `card.refineMessage` 和方向稿首图文件名。
- Produces: 固定中文模型消息，明确同描述精修且禁止参考图/编辑参数。

- [ ] **Step 1: 修改组件测试期望并确认红灯**

```ts
expect(setDraft).toHaveBeenLastCalledWith(
  '请按 design/images/mockup-1.png 这一版方向精修：复用该方向稿原 description，调用 ui_mockup 时使用 fidelity=high-fidelity，并省略 fastPreview、reference、baseImage、editNote；文件名只用于识别方向，不作为 reference/baseImage 参数。',
)
```

```bash
pnpm exec vitest run packages/tool-ui-mockup/tests/toolview.client.spec.tsx -t 'argsRaw 携带 fastPreview'
```

Expected: FAIL；实际仍是旧消息。

- [ ] **Step 2: 更新中英文词典中的固定中文消息**

把 `zh` 和 `en` 的 `card.refineMessage` 同时改为上述字符串。固定中文是既有模型交互契约，不把英文 UI 消息改成英文。

- [ ] **Step 3: 运行精修消息全组**

```bash
pnpm exec vitest run packages/tool-ui-mockup/tests/toolview.client.spec.tsx
```

Expected: PASS；按钮可见性及确认、选版、反馈固定中文测试保持通过。

- [ ] **Step 4: 同步文档并提交**

产品指南和架构说明写明文件只用于指代方向，OpenAI 兼容精修不传参考图。在 `FPR-03` 增加“首次调用不得携带 reference/baseImage/editNote，不接受先失败再恢复”。

```bash
pnpm exec prettier --check packages/tool-ui-mockup/src/client/locales.ts packages/tool-ui-mockup/tests/toolview.client.spec.tsx docs/guides/product-guide.md docs/architecture/overview.md docs/testing/v0.3.0/browser-cases.md
git diff --check
git add packages/tool-ui-mockup/src/client/locales.ts packages/tool-ui-mockup/tests/toolview.client.spec.tsx docs/guides/product-guide.md docs/architecture/overview.md docs/testing/v0.3.0/browser-cases.md
git commit -m "fix(tool-ui-mockup): 约束方向稿精修调用参数"
```

### Task 4: 产品修复全仓验证

**Files:**

- Verify only: repository working tree and the three preceding commits.

**Interfaces:**

- Consumes: 三项已提交修复。
- Produces: 浏览器复测使用的唯一代码候选提交。

- [ ] **Step 1: 运行全仓门禁**

```bash
pnpm test
pnpm typecheck
pnpm lint
pnpm format:check
pnpm build
```

Expected: 全部退出码0；测试无失败或未处理警告。

- [ ] **Step 2: 核对提交与工作区**

```bash
git log -4 --oneline
git status --short
git rev-parse HEAD
```

Expected: 三个原子修复提交紧随计划/设计提交；工作区为空。将完整提交写入本地执行报告，不提交报告。
