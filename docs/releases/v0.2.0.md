# 0.2.0 发布检查清单

> 状态：**v0.2.0 前置校验通过**（2026-09-07；浏览器 ANN/FPR/REG 25/25，自动化门禁通过；
> npm 正式发布未执行）。
> 验收环境：`http://127.0.0.1:3080/`；工作区 `qa-ui-mockup-crm`；Codex 内置浏览器；
> 桌面视口 1296×886；DSH Web 从该工作区目录启动。

## 本版本内容

- **M5 标注式精准反馈**：卡片图片点击进标注弹窗（矩形/画笔/箭头、自动编号、1x/2x、三色、
  撤销/清空、焦点陷阱 + Esc），编号归一化坐标投影 + 意见随消息发出；提示词规则 +
  `baseImage` 标注图硬防护；纯几何模块 `src/annotation.ts`（node 环境单测）；
- **M6 生成加速**：`fastPreview` 方向稿参数（仅 high-fidelity 生效；与线框组合忽略并说明）、
  `draftModel` 方向稿模型档（解析顺序：显式 model → draftModel → wireframeModel → Provider 默认）、
  历史 `fastPreview` 字段 + 「方向稿」标签 + 「只看方向稿」服务端过滤、方向稿设锚一次性提示、
  「按这版精修」按钮（仅 `high-fidelity + fastPreview=true` 可见；线框即使原始参数带
  `fastPreview=true` 也不显示）；
- **M7 火山多图并发**：未合入——本机无 `ARK_API_KEY`，无法按提案门槛制实测（耗时改善 ≥30%
  且 429 不增），保持串行；后续具备凭据时补测再议；
- **S0 结论**：`createDraftImages` 三档判定为第 ② 档（运行时可用、类型面未公开），0.2.1
  标注图附件路径附退出条件；百炼 API 编辑能力作为 0.2.1 候选，但 0.2.0 插件编辑调用明确返回
  `NOT_IMPLEMENTED` 并回退整体重生成；`enable_thinking` 开关存在（落地与否待产品决策），见
  `docs/references/dashscope-image-edit-and-thinking-mode.md`。

## 自动化门禁

| 门禁                 | 结果                       |
| -------------------- | -------------------------- |
| `pnpm test`          | 12 个测试文件、180 项通过  |
| `pnpm typecheck`     | 通过                       |
| `pnpm lint`          | 通过                       |
| `pnpm format:check`  | 通过                       |
| `pnpm build`         | 4 个 TypeScript 包构建通过 |
| `pnpm pack:all`      | 通过，生成 5 个 0.2.0 包   |
| npm dry-run          | 待执行                     |
| npm 正式发布与回读   | 待执行                     |
| 公开 bundle 干净安装 | 待执行                     |

## 浏览器前置校验

用例来源：[0.2.0 浏览器回归手册](../testing/v0.2.0/browser-cases.md)。

| 分组 | 结果  | 关键证据                                                                 |
| ---- | ----- | ------------------------------------------------------------------------ |
| ANN  | 13/13 | 三工具标注、纯文字退化、焦点/Esc、英文界面、路径防护、多轮最新意见均通过 |
| FPR  | 7/7   | 方向稿、精修、三档模型、历史过滤、锚点提示、线框忽略语义均通过           |
| REG  | 5/5   | 三反馈按钮、原图、续时、四页设置、损坏历史容忍均通过                     |

补充验证与恢复：

- ANN-11 临时移走既有双图卡片第一张图，弹窗显示“图片加载失败，无法标注。”与“取消”，页面不白屏；
  恢复后 SHA-256 为
  `88c92a52491a3d13d8d84584edde6226d99a9b50d9e6b72adac3b49f0068308c`；
- REG-05 用 2 条有效记录、半截 JSON、垃圾文本、空行和 `null` 组成 6 行夹具；历史页只显示
  2 条有效记录且按新到旧排序。原 51 行历史恢复后 SHA-256 为
  `34ee5276c94848389de6338202fed10874dfd3982b0d9e45cdb5f4fb6dbea8bb`；
- REG-04 在重新构建并从 `qa-ui-mockup-crm` 启动 DSH 后，概览“如何反馈”显示新版标注入口文案；
- 交叉复核发现并修复 FPR-03/FPR-04 边界：线框调用携带 `fastPreview=true` 时仍保留忽略说明，
  但不再误显示“按这版精修”；高保真方向稿按钮保持可见。

本轮生成与反馈链路使用的 6 张文件：

- `mockup-1788762303682-cb910896-1.png`：方向稿，`qwen-image-3.0`；
- `mockup-1788762450473-6dce5a78-1.png`：按方向精修，`qwen-image-3.0-pro`；
- `mockup-1788762705119-59a5e922-1.png`：线框 + fastPreview 忽略语义；
- `mockup-1788762893717-72ccd76e-1.png`：ANN 首轮标注后的整体重生成；
- `mockup-1788763163469-efa23cf5-1.png`：ANN 最新一轮标注后的整体重生成；
- `mockup-1788763192168-06ca33c2-1.png`：ANN-07 无标注纯文字反馈后的整体重生成。

真实环境限制：DashScope 编辑调用返回 `NOT_IMPLEMENTED`，Agent 按规则改用整体重生成；Qwen
会话模型遇到 token-plan 周配额 429（重置时间 2026-09-10 10:12 UTC），剩余调度临时改用
DeepSeek-V4-Flash，验收结束后两个会话均恢复为 Qwen3.8 Max · Xhigh。该 429 不属于插件故障。

### 提交前浏览器冒烟（2026-09-07）

- 从 `qa-ui-mockup-crm` 启动 DSH Web，Codex 内置浏览器打开两个既有验收会话；页面加载正常，
  两个会话均为 Qwen3.8 Max · Xhigh 且无运行中任务；
- UI 草图四页均可进入：概览显示新版标注入口；Provider 为 DashScope；三档模型分别为
  `qwen-image-3.0`、`qwen-image-3.0-pro`、跟随提供方默认；生成偏好控件正常；
- 生成历史显示“共 51 条”，最新记录与原图入口正常；
- 点击有效图片可打开标注弹窗，矩形/画笔/箭头、三色、撤销/清空、1x/2x、意见输入、取消和
  打开原图入口齐全；画笔切换和 2x → 1x 恢复正常，取消后页面状态正常；
- 高保真方向稿显示“按这版精修”，普通精修稿不显示；线框 + `fastPreview=true` 结果保留
  “已忽略 fastPreview”说明且不显示精修按钮。

## 发布物修复

- 根因：`packages/tool-ui-mockup/src/client/locales.ts` 已是新版文案，但运行和 tarball 读取的
  `lib/client.js` 较旧；原 `pack:all` 直接打包现有 `lib/`，允许陈旧构建进入发布物；
- 修复：`pack:all` 在清理/打包前强制执行 `pnpm build`；新增行为测试，在隔离临时目录执行真实
  脚本并断言第一个 `pnpm` 调用是 `build`，而不是只检查脚本文本；
- 发布物核对：`dsh-tool-ui-mockup-0.2.0.tgz` 含新版概览文案且不含旧文案；bundle README
  安装命令为 0.2.0，三个 bundle 依赖均为 `^0.2.0`；
- 文档同步：bundle README 与产品指南安装命令统一为 0.2.0，产品指南发布链接指向本清单；
- 未引入新依赖。

## 新增测试覆盖（相对 0.1.3 的 150 项，净增 30 项）

- `annotation.spec.ts`（19 项）：归一化包围盒、编号、三工具文本投影、1x/2x 与 DPR 坐标映射、
  徽标布局、时间戳固定格式、消息模板（含 `$&` 类替换模式回归）；
- `annotate.client.spec.tsx`（3 项）：卡片进弹窗 + Esc、矩形标注 → 编号坐标进模型消息、
  无标注退化纯文字反馈；
- `toolview.client.spec.tsx`（+2 项）：精修按钮可见性（含截断/损坏 argsRaw）与固定中文消息；
  同时覆盖线框 + fastPreview 的组合边界；
- `composition.spec.ts`（+2 项）：fastPreview 模型解析 + 历史标记 + 方向稿设锚提示；
  线框档忽略语义；标注图 `baseImage` 硬防护（+1 项）；
- `prefs.spec.ts`（+1 项）：`fastPreview` 历史字段新旧行兼容与损坏值防御。
- `release-metadata.spec.ts`（2 项）：真实执行 `pack:all` 时构建先于所有打包动作；五个发布包版本
  与根版本一致。

## 正式发布前仍待执行

1. 若把“性能收益”作为发布硬门槛，补足方向稿与精修档各 ≥3 轮并记录耗时中位数；
2. 若具备当前火山凭据，补齐同一 0.2.0 构建下的火山标注反馈 → 编辑链路复验；
3. 执行 npm dry-run；
4. 发布：五包按序（`dsh-image` → `dsh-image-dashscope` → `dsh-image-volcengine` →
   `dsh-tool-ui-mockup` → bundle），复刻 0.1.3 清单；
5. 发布后完成 registry 回读、公开 bundle 干净安装，并更新本文件的基线提交号。

## 已知限制

- 标注反馈的「局部修改」效果依赖提供方编辑能力：百炼链路为「更精确地描述一次整体重画」，
  产品文档不承诺「只改这块、其余不动」；
- 标注图附件随消息发出（多模态直接看标记）为 0.2.1 内容；
- 标注圈选不支持键盘操作（无障碍兜底为纯文字意见路径）；
- `setDraft` 覆写用户正在输入草稿为既有行为，本版本未扩大；
- 火山多图仍为串行（M7 未实测）。
