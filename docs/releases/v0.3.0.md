# v0.3.0 发布记录

[English](../en/releases/v0.3.0.md)

> 记录日期：2026-09-17
> 验收候选提交：`83cc6cc`（已通过 `294f5d4` 合并至本地 `develop`）
> 正式发布提交：`9107195cc22fae326a44135afeddd1cb53026053`
> Git 标签：[v0.3.0](https://github.com/mackwan84/dsh-ui-mockup/releases/tag/v0.3.0)
> 状态：**已正式发布。**

## 候选验收结论

- P0：94 条通过、1 条按单工作区验收约束不适用，零失败、零阻塞、零未执行；
- DashScope、Volcengine、OpenAI 兼容网关均完成隔离 UI 凭据写入、真实生成与 UI 清除；
- Volcengine 仅承诺高保真与整图编辑。`fidelity=wireframe` 会在工具层明确拒绝并引导切换 Provider；
- 浏览器覆盖标注删除、锚点、三方参考图/自动锚点、语言主题视口矩阵和计时关键边界；
- 原始截图、请求采样与服务日志保存在未提交的 `.artifacts/` 中，不含密钥值。

## 工程门禁

| 检查                                         | 结果                           |
| -------------------------------------------- | ------------------------------ |
| `pnpm test`                                  | 14 个测试文件，301/301 通过    |
| `pnpm typecheck`                             | 通过                           |
| `pnpm lint`                                  | 通过                           |
| `pnpm format:check`                          | 通过                           |
| `pnpm build`                                 | 通过                           |
| `pnpm pack:all`                              | 通过，生成六个 `0.3.0` tarball |
| 六包 `npm publish ./dist/<包>.tgz --dry-run` | 通过                           |
| 全新临时目录安装六个 tarball                 | 通过，六包版本均为 `0.3.0`     |

## 正式发布事实

- 发布 MR：[PR #8](https://github.com/mackwan84/dsh-ui-mockup/pull/8) 已合并，远端 `main` 提交为 `9107195cc22fae326a44135afeddd1cb53026053`；
- 从该提交的隔离工作树重新生成唯一一套六包 tarball，并对每包执行 `npm publish --dry-run` 与本地 tarball 闭包安装；
- 已按依赖顺序发布六包，npm `latest` 均为 `0.3.0`；registry `dist.shasum` 依次为：
  - `@mackwan84/dsh-image`：`23871294ba649599b4cb923ac6e9f71f9364c1f9`；
  - `@mackwan84/dsh-image-dashscope`：`db7fec4d8d455d121a6216dc6f3cfbb82eedd7fd`；
  - `@mackwan84/dsh-image-volcengine`：`eac989d0046307d0dfc7048548f3d0cdfa86f06a`；
  - `@mackwan84/dsh-image-openai-compat`：`dde0ab519018a99141d6639ce509e58fdf3999af`；
  - `@mackwan84/dsh-tool-ui-mockup`：`b29f1613f7ecdd2cf75d326cd420d4b7b93e0bf8`；
  - `@mackwan84/dsh-ui-mockup-bundle`：`d9f011d67b66df37b46a02553fc572523ff7ec25`；
- 全新公网临时目录执行 `npm install @mackwan84/dsh-ui-mockup-bundle@0.3.0 --registry=https://registry.npmjs.org` 后，六包均解析为 `0.3.0`，`npm audit --omit=dev --audit-level=high` 为零漏洞；
- 已创建同提交注解标签 `v0.3.0` 并推送；GitHub Release 已发布，非草稿、非预发布。

## 已知验证边界

- 用户可见 UI 的浏览器验收证据来自候选提交 `83cc6cc`。`83cc6cc` 到合并前候选 `2c2f35e` 的产品代码无变更，远端 `main` 与该候选源码树一致；本次发布重新构建了该源码树。发布后未新增另一轮 UI 交互采集。
