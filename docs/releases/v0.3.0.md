# v0.3.0 发布记录

> 记录日期：2026-09-17
> 候选提交：`83cc6cc`（当前候选分支 `codex/v0.3.0-release-closure`）
> 状态：**候选验收完成，尚未正式发布。**

## 候选验收结论

- P0：94 条通过、1 条按单工作区验收约束不适用，零失败、零阻塞、零未执行；
- DashScope、Volcengine、OpenAI 兼容网关均完成隔离 UI 凭据写入、真实生成与 UI 清除；
- Volcengine 仅承诺高保真与整图编辑。`fidelity=wireframe` 会在工具层明确拒绝并引导切换 Provider；
- 浏览器覆盖标注删除、锚点、三方参考图/自动锚点、语言主题视口矩阵和计时关键边界；
- 原始截图、请求采样与服务日志保存在未提交的 `.artifacts/` 中，不含密钥值。

## 工程门禁

| 检查                                         | 结果                           |
| -------------------------------------------- | ------------------------------ |
| `pnpm test`                                  | 14 个测试文件，301/301 通过    |
| `pnpm typecheck`                             | 通过                           |
| `pnpm lint`                                  | 通过                           |
| `pnpm format:check`                          | 通过                           |
| `pnpm build`                                 | 通过                           |
| `pnpm pack:all`                              | 通过，生成六个 `0.3.0` tarball |
| 六包 `npm publish ./dist/<包>.tgz --dry-run` | 通过                           |
| 全新临时目录安装六个 tarball                 | 通过，六包版本均为 `0.3.0`     |

## 尚未执行的正式发布步骤

1. 将候选合并到 `develop`，创建并合并 `develop` 到远端默认分支的发布 MR；
2. 从合并后的远端默认分支重新生成并核验唯一 tarball；
3. 发布 npm、回读 `latest` 与 tarball 哈希；
4. 创建同提交的注解 Git 标签和 GitHub Release；
5. 回填上述公开发布事实。
